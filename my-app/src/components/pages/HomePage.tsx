import { SxProps, Typography } from "@mui/material";

export type CopyrightProps = {
  sx: SxProps;
};

const HomePage = () => {

  return (
    <Typography variant="h1" color="text.primary" mb="32px" >HomePage</Typography>

  );
};

export default HomePage;